inventory: openstack
working_dir: "{{ lookup('env', 'WORKSPACE') }}"
local_working_dir: "{{ lookup('env', 'LWD') }}"

# Define as needed
undercloud_user: centos
ssh_user: "{{ undercloud_user }}"
non_root_user: "{{ undercloud_user }}"
overcloud_user: "{{ undercloud_user }}"
#os_username: fuser
#os_password: secret
#os_tenant_name: fuser
#os_auth_url: https:/cool_cloud.lc:5000/v2.0
#cloud_name: cool_cloud
#overcloud_key: '{{ working_dir }}/fuser.pem'
#undercloud_key: '{{ working_dir }}/fuser.pem'
